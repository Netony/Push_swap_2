int		push_swap_test(t_vars *vars, t_list **cmd, char *s);
int		push_swap_operation(t_vars *vars, char *s);
void	ft_lstswap(t_list *n1, t_list *n2);
void	ft_lstinsert_back(t_list *prev, t_list *new);
int		ft_putcmd(t_list *cmd);
